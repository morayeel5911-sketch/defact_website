{
  "name": "comfy-asset-sync",
  "version": "1.0.0",
  "description": "ComfyUI ↔ Next.js Asset Sync Pipeline",
  "scripts": {
    "sync": "python3 scripts/sync_comfy-assets.py --mode=full --optimize",
    "sync:dry": "python3 scripts/sync_comfy-assets.py --mode=full --dry-run",
    "watch": "python3 scripts/watch-comfy-output.py",
    "watch:once": "python3 scripts/watch-comfy-output.py --once",
    "manifest:gen": "python3 scripts/generate_manifests.py"
  },
  "dependencies": {},
  "devDependencies": {}
}
